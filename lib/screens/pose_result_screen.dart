import 'package:flutter/material.dart';

class PoseResultScreen extends StatelessWidget {
  const PoseResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Pose Results")),
      body: const Center(child: Text("Phase 1 Coming...")),
    );
  }
}
